# Dependências do backend no modo Docker/produção (Dockerfile faz `pip
# install -r requirements.txt`). Para o launcher desktop standalone, veja
# requirements-desktop.txt.
-r requirements-core.txt
gunicorn==22.0.0
