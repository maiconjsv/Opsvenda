services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "${APP_PORT:-5000}:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY}
    volumes:
      - app_instance:/app/instance

volumes:
  app_instance:
